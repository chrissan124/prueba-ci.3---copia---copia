function sumOperation(a, b) {
  return parseInt(a) + parseInt(b);
}

function multiplicationOperation(a, b) {
  return parseInt(a) * parseInt(b);
}

module.exports = { sumOperation, multiplicationOperation };
